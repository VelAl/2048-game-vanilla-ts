import { defineConfig } from 'vite';

export default defineConfig({
  server: {
    /* 
      Automatically opens the app in a new browser tab when the dev server is started.
    */
    open: true,
  },
});
